Compatibility facades for generated Lean live here once the emitter stops
hard-coding builtin special cases.

The current Moonshine milestone still compiles with the generated builtin
modules, so this directory is a reserved landing zone rather than an active
dependency.
